fastapi>=0.141.1
uvicorn[standard]>=0.52.4
SQLAlchemy>=2.0.52
pydantic[email]>=2.13.5
pydantic-settings>=2.15.0
python-jose[cryptography]>=3.5.0
passlib[bcrypt]>=1.7.4
# passlib 1.7.4 (unmaintained since ~2020) can't detect bcrypt>=4.1's version
# info and crashes at startup; bcrypt<4.1 is unaffected and its wheel covers
# every Python 3.6+ release (it's built for the stable "abi3" ABI), so this
# doesn't reintroduce the wheel-availability problem this file exists to avoid.
# Confirmed 2026-09-11: bcrypt 5.0.0 reproduces the crash directly (passlib's
# CryptContext throws AttributeError: module 'bcrypt' has no attribute
# '__about__', then falls into a broken fallback that raises a misleading
# "password cannot be longer than 72 bytes" ValueError instead of the real
# error) — Dependabot's grouped update PR #3 tried to widen this to <5.1 and
# its own CI test job failed as a direct result. Do not raise this bound
# without upgrading passlib (or replacing it) first, and re-verifying.
bcrypt<4.1
python-multipart>=0.0.32
alembic>=1.19.2
slowapi>=0.1.10
httpx>=0.28.1
python-dotenv>=1.2.3
# Inactive until SENTRY_DSN is set (see app/core/observability.py) — a real
# Sentry account/project is a manual step (Technical Implementation Plan
# step 1.c.ii). Pure-Python-ish SDK, no wheel-build risk like firebase-admin,
# so unlike that one it's fine to always install rather than keep separate.
sentry-sdk[fastapi]>=2.68.1
# TOTP MFA for admin roles (Technical Implementation Plan step 2.a.iv) —
# pure Python, RFC 6238, no compiled deps, no external account needed.
pyotp>=2.10.0
# University SAML SSO (Technical Implementation Plan step 2.b.i). Pulls in
# xmlsec (Python bindings for libxmlsec1, a C library) transitively — this
# has historically been a painful, source-build-only dependency, but as of
# writing it ships prebuilt wheels for manylinux and macOS (confirmed for
# this repo's Python 3.13 target on both), so it doesn't reintroduce the
# wheel-availability problem this file otherwise avoids. Re-check this if
# installs ever start failing here.
python3-saml>=1.16.0
# Real Stripe Connect/PaymentIntent integration (Technical Implementation
# Plan Workstream 3) — moved here from requirements-integrations.txt now
# that app/services/stripe_*.py import it at module load time (the app
# fails to boot without it, unlike the truly-optional firebase-admin,
# which nothing imports yet). Pure Python (an HTTP client wrapper, no C
# extension), same "no wheel-build risk" reasoning as sentry-sdk above.
# Held back from Dependabot's PR #3 (which wanted >=15.6.1, a major-version
# jump from this pin) pending real verification against this file's actual
# usage in app/services/stripe_*.py — no test-account-free path exists to
# exercise the real Stripe API automatically, so a breaking change in a
# major bump wouldn't be caught until staging. Revisit deliberately, not
# as part of a routine dependency sweep.
stripe>=10.12.0
# Invoice/receipt PDF generation (Technical Implementation Plan 3.d.iii) —
# pure Python (py3-none-any wheel, confirmed before adding), no C-extension
# build risk, unlike weasyprint (needs system cairo/pango) or reportlab
# (heavier, less actively maintained for this use case).
fpdf2>=2.8.8
