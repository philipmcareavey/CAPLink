# Real third-party integration for the one feature still left as a
# placeholder in the code that needs a heavy, wheel-risky dependency (see
# README > "What's stubbed vs. production-ready"): Firebase Cloud Messaging
# (mobile push). Kept out of requirements.txt — firebase-admin pulls in a
# large, compiled dependency tree (grpcio, etc.) that's a common source of
# wheel-build failures on newer/less common Python setups, for no benefit
# to the demo. (Stripe moved to requirements.txt once app/services/stripe_*.py
# started importing it at module load time — see that file's comment.)
#
# Install with: pip install -r requirements.txt -r requirements-integrations.txt
firebase-admin>=6.5.0
