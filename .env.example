# Development environment template (copy to `.env`). For staging/production,
# use `.env.staging.example` / `.env.production.example` instead — Settings
# (app/core/config.py) picks the right file automatically from ENVIRONMENT,
# and rejects dev's placeholder SECRET_KEY/DATABASE_URL outside development.

# --- Core ---
ENVIRONMENT=development
SECRET_KEY=change-this-to-a-long-random-string
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=30

# --- Database ---
# SQLite for local dev (zero setup):
DATABASE_URL=sqlite:///./caplink.db
# Postgres for production, e.g.:
# DATABASE_URL=postgresql://caplink_user:password@localhost:5432/caplink

# --- CORS (comma separated) ---
CORS_ORIGINS=http://localhost:3000,https://app.caplink.io,capacitor://localhost

# --- Payments ---
# Leave empty for local dev — with no key set, app/services/stripe_dev_mode.py
# simulates every Stripe call (Connect onboarding, PaymentIntents, capture)
# with fake-but-consistent IDs instead of hitting the real API, so the
# contract/milestone flow keeps working with zero setup, same as everywhere
# else in this file. This used to hold a placeholder-looking value
# (sk_test_xxx) that nothing ever actually read — now that Workstream 3
# reads it for real, a non-empty-but-fake value here would make dev try to
# call the real Stripe API with garbage credentials instead of simulating.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
PLATFORM_FEE_PERCENT=12.5

# --- Push notifications (mobile) ---
FIREBASE_CREDENTIALS_JSON=./firebase-service-account.json

# --- Observability ---
LOG_LEVEL=DEBUG
# Leave empty to disable error tracking (the default everywhere) — set to a
# real Sentry DSN once a Sentry project exists (see .env.staging.example).
SENTRY_DSN=

# --- Auth hardening ---
# Used to build the link inside verification emails (see app/services/email.py
# — no real ESP wired up, so it only ever appears in the logs right now).
# Registration in development auto-verifies and skips this entirely anyway.
PUBLIC_APP_URL=http://localhost:8000
EMAIL_VERIFICATION_TOKEN_EXPIRE_HOURS=24
ACCOUNT_LOCKOUT_THRESHOLD=5
ACCOUNT_LOCKOUT_BASE_MINUTES=5
# HIBP's Pwned Passwords check needs outbound internet access — turn off if
# working offline (complexity rules still apply either way).
PASSWORD_BREACH_CHECK_ENABLED=true

# --- API hardening & abuse prevention ---
# Leave empty to disable bot-protection on registration (the default
# everywhere, including here) — set once a real hCaptcha account/site
# exists (see .env.staging.example). See app/services/captcha.py.
HCAPTCHA_SECRET_KEY=
CAPTCHA_ENABLED=true
MAX_REQUEST_BODY_BYTES=2097152

# --- Multi-tenant licensing ---
DEFAULT_UNIVERSITY_TRIAL_DAYS=30
